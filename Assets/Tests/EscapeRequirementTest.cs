using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class EscapeRequirementTests
{
    //private EscapeTrigger escapeTrigger;

    //[SetUp]
    //public void Setup()
    //{
    //    // Set up the escape trigger and assign required fields
    //    var escapeGameObject = new GameObject();
    //    escapeTrigger = escapeGameObject.AddComponent<EscapeTrigger>();
    //    escapeTrigger.totalCarParts = 5;
    //}

    //[Test]
    //public void CannotEscapeWithoutAllCarParts()
    //{
    //    // Simulate collecting fewer than the required car parts
    //    escapeTrigger.CollectCarPart();
    //    escapeTrigger.CollectCarPart();

    //    // Check if escape condition fails without all parts
    //    Assert.IsFalse(escapeTrigger.CanEscape(), "Player should not be able to escape without all car parts.");
    //}

    //[Test]
    //public void CanEscapeWithAllCarParts()
    //{
    //    // Simulate collecting the required number of car parts
    //    for (int i = 0; i < escapeTrigger.totalCarParts; i++)
    //    {
    //        escapeTrigger.CollectCarPart();
    //    }

    //    // Check if escape condition is met
    //    Assert.IsTrue(escapeTrigger.CanEscape(), "Player should be able to escape with all car parts collected.");
    //}
}
